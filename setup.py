from setuptools import setup, find_packages

setup(
        name='powerline-vaulted-segment',
        packages=find_packages(),
        namespace_packages=['powerline_vaulted_segment'],
        version='0.1.0'
        )
